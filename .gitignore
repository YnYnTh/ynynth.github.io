# Quarto build output — regenerated by CI, never commit
/_site/
/.quarto/

# Do commit /_freeze/ : it caches R chunk results so CI doesn't need R

# macOS
.DS_Store

# R
.Rhistory
.RData
.Rproj.user/
